// eslint-disable-next-line no-shadow
enum FieldType {
    TEXT = 'text',
    NUMBER = 'number',
    URL = 'url',
    PASSWORD = 'password',
    SELECT_BOX = 'selectbox',
    CHECKBOX = 'checkbox',
}

export default FieldType;
